#테스트 코드는 test 내 resources가 우선권을 갖는다.
#스프링 부트는 별도의 설정이 없으면 디폴트로 메모리 모드를 통해 테스트를 진행한다.
spring:
#  datasource:
#    url: jdbc:h2:mem:test #메모리 모드로 테스트
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true


logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace #쿼리 파라미터 추적 가능
